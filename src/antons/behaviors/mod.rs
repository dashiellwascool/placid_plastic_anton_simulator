/// This folder should contain components with unique behaviors...
use bevy::prelude::*;

// This function should register them
pub fn register_behaviors(app: &mut App) {
    // app.register_systems...
}